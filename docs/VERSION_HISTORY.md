# GameBalanceTool V3 版本历史

## 版本号规则

项目统一使用语义版本号：`MAJOR.MINOR.PATCH`。

| 层级 | 递增条件 | 示例 |
|---|---|---|
| `MAJOR` 主版本 | 数据结构或核心架构不兼容，需要迁移说明 | `4.0.0` |
| `MINOR` 功能版本 | 新增可见功能、核心模块、页面级能力，兼容旧数据 | `3.10.0` |
| `PATCH` 修订版本 | Bug 修复、UI 微调、文案修正、测试补齐，不改变功能边界 | `3.10.17` |

维护规则：
- 当前版本单一来源为 `src/core/project-versioning.js` 的 `PROJECT_VERSION`。
- 页面头部、导出文件名、工程封包、导入迁移目标、测试断言都必须跟随 `PROJECT_VERSION`。
- 每次本地验收通过并同步 GitHub 前，若变更属于功能版本或修订版本，必须先更新本文件。
- Git 标签建议使用 `vX.Y.Z`，例如 `v3.10.17`。

## v3.10.17 - 投放卡片三列修订版

目标：修复“投入回报对比”页系统投放卡片一行四张导致卡片拥挤的问题，默认改为一行三张，并保留窄屏自适应。

修复内容：

| 模块 | 变更 |
|---|---|
| 投入回报对比 | `#roi-sys-grid` 从复用 flex 卡片流改为三列 CSS Grid |
| 投入回报对比 | 900px 以下自动两列，620px 以下自动单列 |
| 投入回报对比 | 仅作用于系统投放卡片，不影响职业设定页复用的 `.roi-sys-grid` |
| 测试防护 | 烟测覆盖系统投放卡片前三张同排、第四张换行 |

验证范围：

- JS 语法检查
- 内联脚本检查
- `npm test`
- 内置浏览器打开投入回报对比页，确认系统投放卡片一行三张

## v3.10.16 - 战斗日志字段拆分修订版

目标：修复“战斗演算”页单回合生命跌落追踪表字段颗粒度过粗的问题，将双方伤害与双方剩余生命拆成独立字段，并在战斗结束时明确给出胜负。

修复内容：

| 模块 | 变更 |
|---|---|
| 战斗演算 | 日志表头由 4 列扩展为 6 列：当前轮次、攻方伤害、守方伤害、攻方剩余生命、守方剩余生命、战场即时态势评估 |
| 战斗演算 | 每回合日志不再把双方伤害与生命值拼接到同一字段 |
| 战斗演算 | 结束回合状态从“战斗结束”改为“攻方胜利”或“守方胜利” |
| 测试防护 | 烟测覆盖日志表头、行列数量和胜负状态 |

验证范围：

- JS 语法检查
- 内联脚本检查
- `npm test`
- 内置浏览器打开战斗演算页，确认日志字段拆分和胜负展示正常

## v3.10.15 - 战力层级输入居中修订版

目标：修复“战斗演算”页战力层级划分组件内输入框文字左对齐的问题，统一改为居中显示。

修复内容：

| 模块 | 变更 |
|---|---|
| 战斗演算 | 新增 `#combat-tier-section .fc{text-align:center}` 局部样式 |
| 战斗演算 | 战力分层、战力阈值、建议付费分层、评级颜色输入框统一居中显示 |
| 测试防护 | 烟测覆盖该组件内所有输入框 `text-align` 均为 `center` |

验证范围：

- JS 语法检查
- 内联脚本检查
- `npm test`
- 内置浏览器打开战斗演算页，确认战力层级组件输入框文字居中

## v3.10.14 - 境界层数冗余修订版

目标：移除“养成系统”页境界修炼系统小境界编辑器右侧重复显示的总层数文本，避免和下方层级列表信息重复。

修复内容：

| 模块 | 变更 |
|---|---|
| 养成系统 | 删除小境界编辑器右侧 `cr-level-count` 冗余展示节点 |
| 养成系统 | 移除 `renderRealms()` 中对已删除节点的写入，避免空节点引用 |
| 测试防护 | 烟测覆盖 `#cr-level-count` 不再出现在页面中 |

验证范围：

- JS 语法检查
- 内联脚本检查
- `npm test`
- 内置浏览器打开养成系统页，确认右侧总层数文本已移除

## v3.10.13 - 职业属性紧凑修订版

目标：修复“职业设定”页职业卡片中属性名和值被左右撑开、阅读距离过大的问题，让战士、法师、通用三类职业属性统一改为紧凑对齐。

修复内容：

| 模块 | 变更 |
|---|---|
| 职业设定 | `.class-stats` 改为内容宽度自适应的两列布局 |
| 职业设定 | `.class-stat-item` 改为属性名右对齐、属性值左对齐的紧凑双列 |
| 测试防护 | 烟测覆盖职业属性名和值左右关系、间距上限和每行两条属性布局 |

验证范围：

- JS 语法检查
- 内联脚本检查
- `npm test`
- 内置浏览器打开职业设定页，确认三类职业属性紧凑对齐

## v3.10.12 - 沙盘属性横排修订版

目标：修复“战斗演算”页沙盘对决中敌我双方属性卡片上下堆叠导致高度过高、阅读密度差的问题，统一改为属性名和值左右同排展示。

修复内容：

| 模块 | 变更 |
|---|---|
| 战斗演算 | `.role-stat` 改为 flex 横向布局，属性名和值同一行展示 |
| 战斗演算 | 敌我双方属性卡片同步压缩高度，避免沙盘区域被无效高度撑开 |
| 测试防护 | 烟测覆盖属性名和值左右关系、垂直居中和卡片高度上限 |

验证范围：

- JS 语法检查
- 内联脚本检查
- `npm test`
- 内置浏览器打开战斗演算页，确认敌我双方属性卡片为左右横排

## v3.10.11 - 曲线库上下布局修订版
目标：修复“曲线库”页曲线列表与右侧预览左右并排导致信息拥挤的问题，改为上下铺满布局。

修复内容：
| 模块 | 变更 |
|---|---|
| 曲线库 | 曲线库主体由通用 `grid-2` 改为 `.curve-library-stack` |
| 曲线库 | 曲线列表和曲线预览改为上下排列 |
| 曲线库 | 曲线预览区抽出 `.curve-preview-panel` 样式，保留原图表 DOM 与绘制逻辑 |
| 测试防护 | 烟测覆盖曲线库表格与预览上下排列、宽度铺满 |

验证范围：
- JS 语法检查
- 内联脚本检查
- `npm test`
- 内置浏览器打开曲线库页，确认列表与预览为上下布局

## v3.10.10 - 投入回报工具条修订版
目标：修复“投入回报对比”页顶部工具条输入框、天数文案和按钮组间距不符合当前排版要求的问题。

修复内容：
| 模块 | 变更 |
|---|---|
| 投入回报对比 | 总预算输入框水平居中，宽度可显示 10 位数字 |
| 投入回报对比 | 去除天数项左侧 label，仅保留 `第1/7天` 格式 |
| 投入回报对比 | 天数上限输入框水平居中，宽度可显示 2-3 位数字 |
| 投入回报对比 | “下一天 / 智能分配 / 重置”改为独立按钮组，收紧按钮间距 |
| 测试防护 | 烟测覆盖输入框居中、宽度、天数文案、按钮间距 |

验证范围：
- JS 语法检查
- 内联脚本检查
- `npm test`
- 内置浏览器打开投入回报对比页，确认顶部工具条排版正常

## v3.10.9 - 投资按钮可读性修订版
目标：修复“投入回报对比”页养成线投资按钮禁用态白底白字、文本不可读的问题。

修复内容：
| 模块 | 变更 |
|---|---|
| 投入回报对比 | `roi-sys-btn` 基础态固定为白底黑字 |
| 投入回报对比 | 禁用态继承白底黑字且透明度恢复为 1，避免文字被淡化到不可读 |
| 测试防护 | 烟测覆盖投资按钮背景色、文字色和透明度 |

验证范围：
- JS 语法检查
- 内联脚本检查
- `npm test`
- 内置浏览器打开投入回报对比页，确认禁用投资按钮为白底黑字

## v3.10.8 - 境界指标横排修订版
目标：修复“养成系统”页境界修炼系统中四个指标卡内部内容上下堆叠的问题，改为 `名称：数值` 的横向展示。

修复内容：
| 模块 | 变更 |
|---|---|
| 养成系统 | `realm-metrics` 四个指标卡保持横向四列展示 |
| 养成系统 | 指标卡内部由上下 label/value 改为同一行左右展示 |
| 养成系统 | 指标文案增加中文冒号，例如 `基础战力：100` |
| 测试防护 | 烟测覆盖四个指标卡同排、卡内 label/value 同排和文本格式 |

验证范围：
- JS 语法检查
- 内联脚本检查
- `npm test`
- 内置浏览器打开养成系统页，确认指标卡横排且内部为一行展示

## v3.10.7 - 击杀矩阵工具条修订版
目标：修复“职业设定”页 1v1 击杀矩阵中模拟场次工具条贴边、偏顶部、控件间距不自然的问题。

修复内容：
| 模块 | 变更 |
|---|---|
| 职业设定 | 1v1 击杀矩阵工具条改为 `.kill-matrix-toolbar` 独立样式 |
| 职业设定 | 工具条增加左右和上下内边距，避免贴边 |
| 职业设定 | 模拟场次输入框、提示文案、重新模拟按钮统一垂直居中 |
| 测试防护 | 烟测覆盖工具条左边距、顶部边距、输入框高度和同线居中 |

验证范围：
- JS 语法检查
- 内联脚本检查
- `npm test`
- 内置浏览器打开职业设定页，确认 1v1 击杀矩阵工具条边距和垂直居中正常

## v3.10.6 - 职业卡片排版修订版
目标：修复“职业设定”页参考等级输入框垂直对齐偏下，以及职业卡片属性内容单列展示过于空旷的问题。

修复内容：
| 模块 | 变更 |
|---|---|
| 职业设定 | “参考等级”行改为独立 `.class-level-row`，固定横向居中对齐 |
| 职业设定 | 职业卡片属性内容改为两列网格，每行展示两条数据 |
| 职业设定 | 属性数值拆分为 label/value，提升卡片内信息密度和扫描效率 |
| 测试防护 | 烟测覆盖参考等级行中心线对齐、输入框高度、职业属性两列排版 |

验证范围：
- JS 语法检查
- 内联脚本检查
- `npm test`
- 内置浏览器打开职业设定页，确认参考等级输入框居中、职业卡片属性两列展示

## v3.10.5 - 投入回报页折叠修订版
目标：修复“投入回报对比”页结果区没有收缩功能、且“各系统投入产出分析”和“策略对比”仍为左右布局的问题。

修复内容：
| 模块 | 变更 |
|---|---|
| 投入回报对比 | “系统投放卡片”接入统一 `section` 折叠体系 |
| 投入回报对比 | “各系统投入产出分析”由普通 `card` 改为 `section` |
| 投入回报对比 | “策略对比 — 同预算不同分配”由普通 `card` 改为 `section` |
| 投入回报对比 | 下方分析区由左右两列改为上下铺满布局 |
| 测试防护 | 烟测覆盖 ROI 页 3 个 section 的折叠按钮、默认展开、手动收起展开和上下布局 |

验证范围：
- JS 语法检查
- 内联脚本检查
- `npm test`
- 内置浏览器打开投入回报对比页，确认各组支持收起和展开，且分析区上下铺满

## v3.10.4 - 付费页折叠修订版
目标：修复“付费模拟”页各结果组没有收缩功能的问题，让该页面主体结果区接入统一 `section` 折叠体系。

修复内容：
| 模块 | 变更 |
|---|---|
| 付费模拟 | “玩家分层付费行为对比”由普通 `card` 改为 `section` |
| 付费模拟 | “选中档位礼包购买路径”由普通 `card` 改为 `section` |
| 付费模拟 | “功能模块投放占比校验”由普通 `card` 改为 `section` |
| 付费模拟 | “复购与付费优势风险诊断”由普通 `card` 改为 `section` |
| 测试防护 | 烟测覆盖付费页 4 个 section 的折叠按钮、默认前三展开、第四收起和手动收起展开 |

验证范围：
- JS 语法检查
- 内联脚本检查
- `npm test`
- 内置浏览器打开付费模拟页，确认各组支持收起和展开

## v3.10.3 - 经济页折叠修订版
目标：修复“经济产出”页各组没有收缩功能的问题，让该页面所有主要配置组接入统一 `section` 折叠体系。

修复内容：
| 模块 | 变更 |
|---|---|
| 经济产出 | “7层货币体系”由普通 `card` 改为 `section` |
| 经济产出 | “贵宾等级特权体系”由普通 `card` 改为 `section` |
| 经济产出 | “货币兑换比率”由普通 `card` 改为 `section` |
| 经济产出 | “日常系统产出与副本活动产出模型配置”由普通 `card` 改为 `section` |
| 测试防护 | 烟测覆盖经济页 4 个 section 的折叠按钮、默认前三展开、第四收起和手动收起展开 |

验证范围：
- JS 语法检查
- 内联脚本检查
- `npm test`
- 内置浏览器打开经济产出页，确认各组支持收起和展开

## v3.10.2 - 战力层级折叠修订版
目标：修复“战斗演算”页战力层级划分表没有收缩功能的问题，让该模块接入统一 `section` 折叠体系。

修复内容：
| 模块 | 变更 |
|---|---|
| 战斗演算 | 战力层级划分分位参考表由普通 `card` 改为 `section` |
| 页面折叠 | 复用现有 `initCollapsibleSections`，自动生成收缩按钮 |
| 测试防护 | 烟测覆盖战力层级表折叠按钮、收起和展开状态 |

验证范围：
- JS 语法检查
- 内联脚本检查
- `npm test`
- 内置浏览器打开战斗演算页，确认战力层级表可收起和展开

## v3.10.1 - 战斗配置布局修订版
目标：修订“战斗演算”页顶部公式配置卡片，取消左右分栏，让公式参数与战力解析结果按策划阅读顺序纵向排列。

核心变更：
| 模块 | 变更 |
|---|---|
| 战斗演算 | “战斗公式模型配置 + 公式预览 + 战力解析器”改为上下单列布局 |
| 战斗演算 | 数据顺序调整为境界/职业预设、公式类型、系数参数、公式预览、战力解析结果 |
| 战斗演算 | 战力解析器标题改为“战力解析结果”，指标卡统一使用 `combat-result-grid` 样式 |
| 测试防护 | 烟测改为校验单列布局、上下顺序和解析结果区可见 |

验证范围：
- JS 语法检查
- 内联脚本检查
- `npm test`
- 内置浏览器打开战斗演算页，确认顶部配置区不再左右分栏

## v3.10.0 - 地图怪物占位版
目标：新增“地图”和“怪物相关”两个页签占位，先完成导航和页面容器，后续再按策划设计填充实际功能。

核心变更：
| 模块 | 变更 |
|---|---|
| 主导航 | 新增“地图”和“怪物相关”两个 Tab |
| 地图 | 新增 `panel-map` 占位页，预留地图结构、区域、资源点与关卡入口配置方向 |
| 怪物相关 | 新增 `panel-monster` 占位页，预留怪物模板、属性、技能、掉落与刷新规则配置方向 |
| 版本体系 | 升级到 `3.10.0`，版本名为“地图怪物占位版” |
| 测试防护 | 烟测覆盖两个新 Tab 的可见性、切换和占位文案 |

验证范围：
- JS 语法检查
- 内联脚本检查
- `npm test`
- 内置浏览器打开 `http://127.0.0.1:8080/`，点击“地图”和“怪物相关”确认占位页可见

## v3.9.0 - 养成精炼整合版

目标：消除养成系统内“精炼增幅计算器”和“三层养成线框架模型”里精炼分支的重复配置入口，把精炼统一收口到三层养成框架。

核心变更：

| 模块 | 变更 |
|---|---|
| 养成系统 | 移除外围独立“精炼增幅计算器”区块 |
| 三层养成线 | `b_refine` 精炼系统保留为唯一可见编辑入口 |
| 精炼分支卡 | 增加“精炼”徽标、材料、单级基础消耗、每级收益、归属模型摘要 |
| 数据来源 | 精炼配置以 `S.cultivations[].branches[]` 为页面配置来源，装备精炼数据继续由该分支同步派生 |
| 默认展开 | 三层养成线默认展开 `l1`，首屏可直接看到精炼分支 |
| 测试防护 | 烟测确认外层 `#refine-panel` 不存在，精炼系统在 `#cult-tree` 内展示 |

验证范围：

- JS 语法检查
- 内联脚本检查
- `npm test`
- 内置浏览器养成系统页采样：无“精炼增幅计算器”，无 `#refine-panel`，精炼系统在三层养成线内显示“材料 / 单级基础消耗 / 每级收益 / 三层养成线”

## v3.8.0 - 战斗沙盘编辑版

目标：把敌我双方属性编辑入口移入“沙盘对决模拟”角色卡，默认只展示属性摘要和编辑按钮，减少上方解析器的信息拥挤。

核心变更：

| 模块 | 变更 |
|---|---|
| 沙盘对决 | 进攻方、守方目标各自新增独立“编辑”按钮，默认展示角色模型、属性摘要和生命条 |
| 属性编辑 | 新增战斗属性编辑弹窗，读取“属性与权重”的全部基础二级属性，支持属性增删后自动同步 |
| 守方配置 | 守方目标可在弹窗内独立修改属性，并保留“按推荐生成”能力 |
| 战力解析器 | 上方解析器不再直接铺满敌我属性输入，只保留伤害、期望输出、回合数等结果指标 |
| 测试防护 | 烟测覆盖守方弹窗编辑、防御改动后伤害降低、推荐生成恢复、双边编辑按钮存在 |

验证范围：

- JS 语法检查
- `npm test`
- 内置浏览器打开 `http://127.0.0.1:8080/`，实测守方防御改为 `9,999` 后沙盘属性展示和伤害结算同步刷新

## v3.8.1 - 付费布局修订版

目标：优化“付费模拟”页的信息阅读顺序，把“选中档位礼包购买路径”和“功能模块投放占比校验”从左右分栏改为上下两组。

核心变更：

| 模块 | 变更 |
|---|---|
| 付费模拟 | 礼包购买路径改为独占整行展示 |
| 付费模拟 | 功能模块投放占比校验改为位于礼包路径下方并水平铺满 |
| 测试防护 | 烟测增加布局断言，确保两个卡片都接近父容器全宽且上下排列 |

验证范围：

- JS 语法检查
- `npm test`
- 内置浏览器付费模拟页布局采样：两个卡片宽度均等于父容器宽度，第二块位于第一块下方

## v3.8.2 - 职业矩阵修订版

目标：修复“职业设定”页 1v1 击杀矩阵和职业面板局部中文乱码。

修复内容：

| 模块 | 变更 |
|---|---|
| 1v1击杀矩阵 | 修复“模拟场次”“范围”“重新模拟”“回合”等可见乱码 |
| 职业预设 | 修复“参考等级”“编辑”“删除”等职业卡按钮与标签 |
| 伤害类型 | 修复“忽略防御”“删除”等可见文案 |
| 职业编辑弹窗 | 修复一级属性名称、弹窗标题、保存/取消按钮，并恢复被乱码注释吞掉的属性预览变量 |
| 测试防护 | 烟测增加 1v1 矩阵中文断言和乱码黑名单 |

验证范围：

- JS 语法检查
- `npm test`
- 内置浏览器职业设定页采样：1v1 矩阵显示“模拟场次 / 重新模拟 / 回合”，职业卡和伤害类型区域无可见乱码

## v3.7.0 - 界面维护版

目标：建立可持续维护的版本号规则，并把近期已验收的付费模拟、战斗演算、属性同步和 UI 折叠能力纳入新的维护基线。

核心变更：

| 模块 | 变更 |
|---|---|
| 版本体系 | 当前版本升级为 `3.7.0`，页面头部显示 `v3.7.0` 与版本名 |
| 工程封包 | `PROJECT_VERSION` 作为导入导出和迁移目标的单一版本源 |
| 付费模拟 | 新增玩家分层、礼包 ROI、复购意愿、付费优势和风险标签评估 |
| 战斗演算 | 沙盘对决改为双方回合制出手，守方目标属性可独立编辑 |
| 属性来源 | 依赖属性的 UI 从“属性与权重”同步生成，支持属性增减联动 |
| UI 表单 | 表单改为左 label、右输入控件布局，长 label 精简 |
| 页面折叠 | `class="section"` 支持展开/收起，每页前三个默认展开，超过三个默认收起 |

验证范围：
- `npm test`
- JS 语法检查
- 浏览器布局采样和战斗/付费/养成页烟测

## v3.7.1 - 装备中文显示修复

目标：修复装备系统配置中“槽位列表”和“品质体系”的乱码显示，并建立防回归校验。

修复内容：

| 模块 | 变更 |
|---|---|
| 装备默认数据 | 重建 `src/data/equipment.js`，恢复武器、头盔、衣服、护腕、腰带、鞋子、戒指、项链等中文槽位名 |
| 品质体系 | 恢复白色、绿色、蓝色、紫色、橙色、红色等品质名称 |
| 旧存档兼容 | 新增 `normalizeEquipmentLabels()`，按稳定 ID 修复旧 IndexedDB/导入数据中已污染的装备名称 |
| 装备面板 | 修复攻击/防御/辅助、编辑/删除、倍率/槽位等显示文本 |
| 测试防护 | 烟测增加装备区中文断言和常见乱码文本黑名单 |

避免复发规则：
- 装备槽位和品质名称以稳定 ID 为准，显示名由 `normalizeEquipmentLabels()` 统一修正。
- 涉及用户可见中文的改动必须跑 `npm test`。
- 如果页面出现 `姝﹀櫒`、`澶寸洈`、`缂栬緫`、`鍊嶇巼` 等典型乱码，烟测必须失败。

## v3.6 - Project 多方案与版本化工程

目标：解决项目数据可导出、可导入、可迁移的问题，并让 ROI/Simulator 的多方案配置从 UI 写死逻辑升级为工程数据。

核心新增：

| 文件 | 说明 |
|---|---|
| `src/core/project-versioning.js` | 定义 `gbt-project` 工程封包、版本号、导入迁移入口和 Project Scenario 工具函数 |
| `src/ui/project-scenario-panel.js` | 新增多方案管理面板，支持新增、编辑、删除、设为当前方案 |

关键改造：

| 模块 | 改造点 |
|---|---|
| `src/core/project-state.js` | 版本升级到 `3.6.0`，`snapshot/restore` 改为版本化入口 |
| `src/export/io.js` | 增加版本化导入导出，保留旧入口兼容 |
| `src/ui/simulator-panel.js` | ROI 初始化和多方案模拟读取 Project Scenarios |
| `src/index-loader.js` | 导入导出按钮接入 v3.6 工程封包 |

兼容范围：

- v3.6 `gbt-project` 工程封包
- v3.1 `gbt-project-state` 快照
- v3 旧快照中的 `v2State.data`
- 部分 v2.1 数据结构

## v3.5 - ROI Strategy Engine 与多方案模拟器

目标：把 ROI 投资模拟从页面逻辑中拆出，形成可复用的 ROI Strategy Engine，并为后续 A/B/C 方案比较准备数据结构。

核心新增：

| 文件 | 说明 |
|---|---|
| `src/engine/roi-strategy.js` | ROI 状态、投资、自动分配、天数推进、策略模拟、摘要输出 |

内置策略：

| 策略 | 说明 |
|---|---|
| 当前手动方案 | 当前用户实际点击结果 |
| 智能 ROI 最优 | 每次选择单位成本收益最高的分支 |
| 均衡补短板 | 优先补低等级分支，再比较 ROI |
| 单点集中堆叠 | 集中堆叠第一个分支，用于验证边际递减 |
| 低成本铺量 | 优先升级低成本分支 |

多方案模拟器增加小 R 试探、标准预算、均衡方案、大 R 拉满等预算档位对比。

## v3.4 - Simulator 数据源统一与分支成长表

目标：

1. Simulator 和 ROI 不再直接依赖 `index.html` 内部临时状态。
2. 分支等级成长明细表从页面逻辑中拆出，成为可复用模块。

核心新增：

| 文件 | 说明 |
|---|---|
| `src/engine/simulator.js` | 生命周期模拟器 |
| `src/ui/simulator-panel.js` | 接管模拟器和 ROI 面板全局函数 |
| `src/ui/branch-growth-table.js` | 分支等级成长明细表 |

关键原则：

- 模拟器数据统一从 `ProjectState.get()` 读取，回退兼容 `window.S`。
- 曲线计算统一调用 `calculateCurveValue(curve, level)`。
- ROI 相关全局函数由 V3 模块覆盖旧 `index.html` 函数。

## v3.3 - 养成分支编辑器模块化

目标：

1. 将养成系统二级分支编辑器从 `index.html` 内联逻辑中拆出。
2. 将曲线绑定下拉框统一为可复用组件。

核心新增：

| 文件 | 说明 |
|---|---|
| `src/ui/components/curve-binding.js` | 曲线选择、曲线名称、曲线对象读取、HTML 转义 |
| `src/ui/cultivation-branch-editor.js` | 分支弹窗、消耗行、属性行、预览、保存、删除 |

关键修复：

- 旧版保存属性收益 `gains` 时只保存 `{ attrId, val }`，导致曲线绑定丢失。
- v3.3 改为 `{ attrId, val, cvId }`，属性收益曲线可在预览表中生效。

兼容策略：

- 保留 `index.html` 原函数。
- ES Module 加载后覆盖同名 `window` 函数。
- 兼容旧 inline `onclick`，避免一次性大拆导致页面不可运行。

## v3.2 - Curve Library 模块化

目标：在 V3.1 状态统一和依赖修复基础上，完成曲线库模块化与动态参数面板接管。

核心新增：

| 文件 | 说明 |
|---|---|
| `src/curve/curve-library.js` | 曲线定义、参数 Schema、计算入口、采样入口 |
| `src/curve/curve-panel.js` | 接管曲线面板、动态参数、预览、引用保护 |

内置曲线：

| 类型 | ID |
|---|---|
| 线性 | `linear` |
| 边际递减 | `diminishing` |
| 对数 | `logarithmic` |
| 平方根 | `sqrt` |
| 幂函数 | `power` |
| 指数增长 | `expo` |
| 指数饱和 | `exponential` |
| S 型 | `sigmoid` |
| 恒定值 | `fix` |
| 阶梯跳跃 | `stair` |

兼容策略：

- 旧版 `S.curves` 保持兼容。
- 初始化时自动补齐缺失参数。
- 旧业务中 `cvVal(c, level)` 已由新模块统一接管。

## v3.1 - 稳定化重构

目标：不新增复杂玩法功能，优先完成去单文件化的第一步、状态统一、Worker 路径修复、CDN 离线化、依赖脚本补齐。

核心新增：

| 文件 | 说明 |
|---|---|
| `src/core/project-state.js` | 统一工程状态入口，兼容旧 `window.S` |
| `src/core/event-bus.js` | 模块间事件通信 |
| `libs/chart-lite.js` | 本地图表兼容层，替代 Chart.js CDN |

关键修复：

| 问题 | 修复 |
|---|---|
| Worker 路径错误 | `../workers/combat.worker.js` 改为 `./workers/combat.worker.js` |
| CDN 依赖 | 移除外部 Chart.js CDN，改本地 `libs/chart-lite.js` |
| 状态入口混乱 | `ProjectState` 接管 snapshot、restore、persist |
| JSON 兼容 | 导入兼容旧结构和 `gbt-project-state` |

残留风险：

- `index.html` 仍然很大。
- 旧内联函数与 V3 模块仍处于过渡共存状态。
- 后续仍需继续拆 Combat、Economy、Shop 等面板。

## 维护结论

V3.1 到 V3.7 的主线是：

```text
状态统一 -> 曲线库 -> 养成分支 -> 模拟器/ROI -> 策略引擎 -> 工程版本化
```

后续新增功能应优先接入：

- `ProjectState`
- `project-versioning`
- `curve-library`
- `roi-strategy`
- `simulator`

避免继续把业务逻辑堆回 `index.html`。
